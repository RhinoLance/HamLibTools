namespace HamLib.Testing;

public class NetTest
{
	/*

		private string _address;
		private int _port;

		NetTest(string address, int port)
		{
			_address = addrses;
			_port = port;
		}

		Assert(string fileName)
		{

			string[] lines = System.IO.File.ReadAllLines(@"C:\Users\Public\TestFolder\WriteLines2.txt");

			foreach (var testLine in lines)
			{

				TestRecord test;
				try
				{
					test = JsonSerializer.Deserialize<TestRecord>(testLine);
				}
				catch (exexption)
				{
					continue;
				}



			}

		}

		private record TestRecord(string command, string response);
		*/

}



